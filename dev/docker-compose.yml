# Development Environment for NaraeClaw Agentic Testing
#
# Use this for:
# - Running the agent in a sandboxed environment
# - Testing dangerous commands safely
# - Developing new skills/integrations
#
# Usage:
#   cd dev && ./cli.sh up
#   or from root: ./dev/cli.sh up
name: naraeclaw-dev
services:
  # ── The Agent (Development Image) ──
  # Builds from source using the 'dev' stage of the root Dockerfile
  naraeclaw-dev:
    build:
      context: ..
      dockerfile: Dockerfile
      target: dev
    container_name: naraeclaw-dev
    restart: unless-stopped
    environment:
      - NARAECLAW_GATEWAY_PORT=42617
      - SANDBOX_HOST=naraeclaw-sandbox
    secrets:
      - source: naraeclaw_env
        target: naraeclaw_env
    entrypoint: ["/bin/bash", "-lc"]
    command:
      - |
        if [ -f /run/secrets/naraeclaw_env ]; then
          set -a
          . /run/secrets/naraeclaw_env
          set +a
        fi
        exec naraeclaw gateway start --port "${NARAECLAW_GATEWAY_PORT:-42617}" --host "[::]"
    volumes:
      # Mount single config file (avoids shadowing other files in .naraeclaw)
      - ../target/.naraeclaw/config.toml:/naraeclaw-data/.naraeclaw/config.toml
      # Mount shared workspace
      - ../playground:/naraeclaw-data/workspace
    ports:
      - "127.0.0.1:42617:42617"
    networks:
      - dev-net

  # ── The Sandbox (Ubuntu Environment) ──
  # A fully loaded Ubuntu environment for the agent to play in.
  sandbox:
    build:
      context: sandbox # Context relative to dev/
      dockerfile: Dockerfile
    container_name: naraeclaw-sandbox
    hostname: dev-box
    command: ["tail", "-f", "/dev/null"]
    working_dir: /home/developer/workspace
    user: developer
    environment:
      - TERM=xterm-256color
      - SHELL=/bin/bash
    volumes:
      - ../playground:/home/developer/workspace # Mount local playground
    networks:
      - dev-net

networks:
  dev-net:
    driver: bridge

secrets:
  naraeclaw_env:
    file: ../.env
